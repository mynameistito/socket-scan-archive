# GitHub Configuration
# Create a personal access token at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_your_personal_access_token_here

# The GitHub organization name where repositories are located
GITHUB_ORG=KillzoneGaming

# Socket.dev Configuration
# Obtain your API token from: https://socket.dev/account/settings
SOCKET_API_TOKEN=your_socket_dev_api_token_here

# Local Configuration
# Directory where repositories will be temporarily cloned
REPOS_BASE_PATH=./temp-repos

# Set to true to run in dry-run mode (preview changes without executing)
DRY_RUN=false

# Optional: API Endpoints (change only if using custom endpoints)
SOCKET_BASE_URL=https://api.socket.dev/v0
GITHUB_BASE_URL=https://api.github.com

# Logging Level: debug, info, warn, error
LOG_LEVEL=info
